// import {getServiceTypes} from "../decorators";
import {BeanFactory} from "../bean-factory";

export class ServiceParser {

    static parse() {
        // getServiceTypes().forEach(serviceType => BeanFactory.create(serviceType));
    }
}